export enum CODE_STATUS {
  SUCCESS = 0,
  ERROR = -1,
  OVERTIME = -2,
  NOAPI = -3,
}
export enum CHANNEL {
  STEAMSHIP = 'steamship',
  OPENAI = 'openai',
}

export enum OpenAITools {
  COMPLETIONS,
  CHAT,
}
